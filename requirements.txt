# Site Migrator — зависимости.
# Cloudflare, Spaceship и Яндекс работают на стандартной библиотеке Python.
# paramiko — SSH-клиент для управления сервером ISPmanager.
# openpyxl — чтение .xlsx при импорте проектов (каждый лист = вкладка).
paramiko>=3.4.0
openpyxl>=3.1.0
