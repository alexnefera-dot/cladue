# Дополнительные названия казино-брендов для «Очистки контента».
# Скопируйте в brands.txt и добавьте свои (по одному на строку; # — комментарий).
#
# Базовый список зашит в коде (src/Content/KnownBrands.php) — тут только дополнения:
# любые упоминания этих брендов в тексте заменяются на переменную
# (латиница → %brand_name_en%, кириллица → %brand_name_ru%), с учётом регистра, границ слова
# и смешения латиницы/кириллицы (STAKE ≡ STAKЕ).
#
# Добавляйте сюда бренды, которые встречаются в ваших шаблонах, но которых нет в базовом списке.
# Осторожно с короткими словами-омонимами (например, обычные слова) — они могут задеть текст.
#
# Примеры:
# newbrand
# ньюбренд
# 7slots
