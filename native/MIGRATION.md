# Pipboy → нативный слой данных (Swift)

Курс выбран: база **вшита в приложение**, шифрование покоя (SQLCipher) ключом из
**Keychain под Touch ID**, **без процесса Node**. Тот же слой переиспользуется на
iPhone + синхронизация. Это и есть старт iPhone-фазы.

## Принцип переноса (важно)
- **Frontend (HTML/JS/CSS в app/public) переиспользуется 1:1 — не трогаем.**
- Меняем только backend: вместо Node-HTTP — Swift за `WKURLSchemeHandler`,
  отвечающий по тому же API-контракту. В JS `fetch('/api/...')` остаётся как есть,
  запрос ловит Swift, выполняет в SQLite и отдаёт тот же JSON.
- Контракт API уже зафиксирован тестами (`app/test/*.test.mjs`, CRUD-набор) —
  переносим обработчики «на паритет», сверяясь с этими ожиданиями.

## Стек
- **GRDB.swift + SQLCipher** (через Swift Package Manager). Зрелая библиотека,
  шифрование встроено. SPM ≠ npm: пакеты компилируются Xcode из исходников,
  без postinstall-загрузок бинарников — то, что ломало Electron, тут не повторится.
- **Keychain** (Security.framework) — хранит ключ базы, доступ под `LAContext`
  (Touch ID). Файл базы = шифр, пока не приложил палец, даже на залогиненном Маке.
- **WKURLSchemeHandler** — мост UI↔данные по тем же путям `/api/*`.

## Этапы
0. **Каркас:** открыть шифрованную БД, ключ в Keychain под палец;
   scheme-handler отдаёт статику + заглушки API. Разовый импорт текущего
   `app/data.db` в зашифрованную базу.
1. **Чтение:** `tree, fin, psy, today, track, calendar, pages` — UI рисуется
   из Swift-данных.
2. **Запись:** create/edit/delete по разделам.
3. **Убрать Node** (ServerProcess) — когда достигнут паритет.
4. **iPhone-таргет** на том же слое + синк (SQLite session/changesets, парный
   ключ для шифрования канала по Wi-Fi).

## Карта backend (что переносим из app/*.js)
| Node-модуль | Зона |
|---|---|
| db.js | схема + миграции |
| core.js | дерево целей, reorder, корзина |
| fin.js | портфель/счета/пассив/обязательства/долги/транзакции/бюджет |
| psy.js | колесо/практики/worklog/замок |
| life.js | рутины/люди |
| cal.js | события/повторы |
| notes.js | страницы/ревизии/AES-шифрование закрытых страниц |
| today.js, track.js, audit.js | дашборд/трекинг/ревизия |

## Честное ограничение среды
Swift в этой среде я не компилирую (нет macOS/Xcode). Поэтому каждый этап:
я пишу код → ты собираешь в Xcode → присылаешь ошибки сборки → правим за итерацию.
Для нативной работы здесь это неизбежно — закладываемся на цикл «сборка ↔ правка».

## Что нужно от тебя один раз (Этап 0)
Добавить пакет в Xcode (пара кликов, не через правку файла проекта вслепую):
File → Add Package Dependencies → `https://github.com/groue/GRDB.swift` →
продукт **GRDB** (вариант со SQLCipher). Хэндлы напишу я.
