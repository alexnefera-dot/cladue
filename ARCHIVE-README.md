# Archive-годные.zip

Годные сайты после фильтрации архива `Archive.zip` из релиза `arhiv-1` (778 сайтов, 4878 страниц).

- **Годных 544**, убрано 234. Убранные в архив не входят.
- Структура: `<тип>/<домен>/<страница>.html`. Типы: `1-стр` (37), `7-стр` (475), `12-стр` (32).
- Сайты групп 8-стр, 9-стр и 10-стр сведены к стандартному набору 7-стр: служебные страницы `privacy`, `contacts`, `about` (и варианты `ruprivacy`, `rucontacts`, `ruabout`) убраны.
- Правок не было: бренды, переменные, ссылки и разметка не менялись.

Причины отбраковки: неполный набор 171, главная без текста 48, дубли файл в файл 24, заглушки 16, дубли по тексту 6 (порог совпадения 60 %).

Отчёт по каждому сайту — `reports/2026-09-07-filter-Archive-big.md` в ветке `claude/cleanup-instructions-templates-y31jlw`.
