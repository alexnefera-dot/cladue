# Выгрузки API: один день /v1/subdomains это ~110 МБ, в историю им нельзя.
# Данные восстанавливаются командой pull_api.py, хранить их в git незачем.
analysis/api/

# Локальное окружение
.env
__pycache__/
*.pyc
