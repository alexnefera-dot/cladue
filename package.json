{
  "name": "yandex-sites-render",
  "private": true,
  "description": "Headless Chromium renderer for yandex-sites visits (tools/render-page.js)",
  "scripts": {
    "check": "node tools/render-page.js --check",
    "install-browser": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.56.0"
  }
}
