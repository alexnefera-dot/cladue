# Референс-корпус конкурента (dorgen)

Экспорт `dorgen_export_24.07.2026` — 9 связок по 7 страниц, снятых с генератора
конкурента «dorgen». Источник целей для **v2** генератора (см. `docs/v2-dorgen.md`).

- Папки `set<id>/` — нормализованные наборы (id из исходных имён экспорта).
- `set232` — аномальный (протёкший «Additional Text»); исключён из коридоров v2.
- Профили сняты в `engine/data-dorgen/donors.json` (`extract-donors.php`),
  коридоры — `engine/data-dorgen/profile.json` (`build-corpus-profile.php`).
- Параметрический разбор: `reports/dorgen-competitor-analysis.html`.
